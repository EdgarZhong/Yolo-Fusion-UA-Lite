# 会话补丁说明（2026-03-12）

## 1. 本次会话完成了什么

1. **新增训练管理器**  
   已新增训练管理器脚本，用于统一加载训练脚本、监控进程、失败后自动切换为断点恢复命令，支持可配置的重试与冷却。  
   关联文件：  
   - `src/trainning/train_manager.py`

2. **建立 CM‑FA 迁移训练脚本**  
   已完成 CM‑FA 迁移训练脚本的路径规范、训练参数与训练管理器对接。  
   关联文件：  
   - `src/trainning/cm_fa_transfer_train.py`

3. **调整 CM‑FA 正式训练超参**  
   已按你的最终要求对齐：  
   - 总轮次 `epochs=160`  
   - 关闭早停 `patience=0`  
   - 冻结策略 `freeze=10`  
   - Mosaic 最后 16 轮关闭 `close_mosaic=16`  
   - 模态失活最后 16 轮关闭 `close_dropout=16`  
   - NMS：`iou=0.75`、`conf=0.25`、`max_det=300`  
   - 输出目录：`models/posttrain/CM-FA-Transferred`

4. **快速冒烟脚本策略调整**  
   快速训练脚本已设置为在进入验证阶段后主动退出，用于确认“进入验证阶段且未爆显存”这一最小目标。  
   关联文件：  
   - `src/trainning/crossmodal_fusion_attention_quick_train.py`

5. **训练管理器具备“一入口自动判断”能力**  
   训练管理器已支持：检测训练是否已完成（基于 `results.csv` 的 epoch），存在 `last.pt` 则自动断点续训，否则自动开始新训。  
   关联文件：  
   - `src/trainning/train_manager.py`  
   - `src/trainning/cm_fa_transfer_train.py`（提供 `run_dir` 与 `total_epochs`）

## 2. 当前关键脚本状态（摘要）

### 2.1 `src/trainning/cm_fa_transfer_train.py`
- **权重来源**：使用 `yolov8n.pt` 作为官方 COCO 预训练权重入口。  
- **训练策略**：冻结前 10 轮 Backbone；Mosaic 与模态失活在最后 16 轮关闭；无早停。  
- **验证参数**：`conf=0.25`，`iou=0.75`，`max_det=300`。  
- **输出路径**：`models/posttrain/CM-FA-Transferred`。  

### 2.2 `src/trainning/train_manager.py`
- **入口协议**：优先读取训练脚本的 `get_train_manager_spec()`，否则回退到 `build_train_command()`/`build_resume_command()`。  
- **完成判断**：若 `run_dir/results.csv` 的最后 epoch 已达到 `total_epochs`，自动判定训练完成并退出。  
- **恢复策略**：若 `resume_ready` 存在则进入 resume 模式，否则保持冷启动。  
- **监控机制**：子进程异常退出会按照重试次数与冷却时间自动重启。  

### 2.3 `src/trainning/crossmodal_fusion_attention_quick_train.py`
- **目标**：仅验证“训练流程可启动并进入验证阶段”。  
- **策略**：在 `on_val_start` 中主动退出，避免 NMS 过慢造成卡顿。  

## 3. 本次会话未执行但已就绪的事项

- **正式训练尚未启动**：所有入口已就绪，仅需运行命令启动训练。  

## 4. 文档目前的欠缺（需要你后续手动补齐）

1. **README 未记录训练管理器**  
   - 尚未说明 `train_manager.py` 的用途、用法与参数。  

2. **README 未记录 CM‑FA 迁移脚本的正式入口**  
   - 未写明 `cm_fa_transfer_train.py` 的具体路径、运行方式与输出目录。  

3. **README 未同步最新超参与冻结策略**  
   - 当前 README 中的阶段五描述与最新要求（`epochs=160`、`freeze=10`、`close_mosaic=16`、`close_dropout=16`、`conf=0.25`、`iou=0.75`）不一致或缺失。  

4. **冒烟测试脚本的退出逻辑未写明**  
   - “进入验证即退出”这一策略未记录到任何文档。  

5. **迁移权重来源的说明不一致**  
   - 迁移训练脚本当前使用 `yolov8n.pt` 作为预训练入口，但部分文档仍指向旧的 FA‑Concat tuned 权重迁移路线。  

## 5. 当前 SRC 目录结构（重点展示）

```
src/
  cfg/
    datasets/
      dual_obb_dronevehicle.yaml
      dual_obb_dronevehicle_origin.yaml
      dual_obb_dronevehicle_train_plus_val.yaml
      ir_obb_dronevehicle.yaml
      ir_obb_dronevehicle_raw.yaml
    model/
      CM-FA_Concat_FPN-PAN_neck.yaml
      FA_Concat_FPN-PAN_neck.yaml
      dualbackbone_CM-FA-Concat.yaml
      dualbackbone_FA-Concat.yaml
      dualbackbone_easy_obb.yaml
      dualbackbone_fusionattention_obb.yaml
  dataset_preprocess/
    clean_mismatch.py
    crop_white_borders.py
    preprocess_obb.py
    verify_obb_preview.py
  testing/
    test_general.py
    test_stability.py
    view_inference.py
    archive/
      benchmark_speed_subset.py
      test_ir_obb.py
  tools/
    build_confusion_matrix_best.py
    plot_single_model_metrics.py
    plot_training.py
    transfer_dual_weights.py
    archive/
      build_summary_table.py
      plot_radar_final.py
  trainning/
    cm_fa_transfer_train.py
    crossmodal_fusion_attention_quick_train.py
    resume_train.py
    train_manager.py
    watchdog_resume.py
    archive/
      FA-Concat-neck_post_train.py
      baseline_quick_train.py
      fa_concat_fpn_pan_quick_train.py
      final_polish_train.py
      final_recall_train.py
      fusion_attention_quick_train.py
      ir_yolov8n_from_scratch.py
      quick_val_latest.py
      train_formal.py
```

## 6. 正式训练的唯一命令（占位）

> 仅记录，不执行  
`python src/trainning/cm_fa_transfer_train.py`

## 7. 统一训练入口定义（Train Manager）

**入口命令**

> 仅记录，不执行  
`python src/trainning/train_manager.py --script src/trainning/cm_fa_transfer_train.py`

**统一入口的判断逻辑（当前实现）**

1. **训练脚本需提供规范信息**：在训练脚本里实现 `get_train_manager_spec()`，返回  
   - `train_cmd`：新训命令  
   - `resume_cmd`：断点续训命令  
   - `resume_ready`：断点文件路径（通常为 `run_dir/weights/last.pt`）  
   - `run_dir`：训练输出目录（包含 `weights/`、`results.csv`、`args.yaml` 等）  
   - `total_epochs`：该训练脚本的总轮次  
2. **完成判断**：若 `run_dir/results.csv` 存在且最后 epoch 已达到 `total_epochs`，判定已完成并退出。  
3. **断点优先**：若 `resume_ready` 存在且 `resume_cmd` 可用，自动走断点续训；否则走新训。  
4. **自动重启**：训练异常退出会自动按重试次数与冷却时间重启，并切换为断点续训。  

**run 目录的含义**

指训练运行输出目录，一般包含：  
- `weights/best.pt`、`weights/last.pt`  
- `results.csv`  
- `args.yaml`  

## 8. 研究助理内容请求响应（完整资料）

### 8.1 `fusion.py` 当前完整现状（关键模块签名）

文件位置：`ultralytics-8.2/ultralytics/nn/modules/fusion.py`

```python
import torch
import torch.nn as nn

from .conv import Conv


class Inception(nn.Module):
    """Inception 多分支模块：强制输入通道能被4整除，输出与输入通道一致"""

    def __init__(self, c1: int):
        super().__init__()
        # 强制检查：输入通道必须为4的倍数，否则抛出异常
        assert c1 % 4 == 0, f"FusionAttention.Inception 输入通道必须能被4整除，当前为 c1={c1}"
        c_branch = c1 // 4
        self.b1 = Conv(c1, c_branch, k=1, s=1)
        self.b2 = Conv(c1, c_branch, k=3, s=1, p=1)
        self.b3 = Conv(c1, c_branch, k=5, s=1, p=2)
        self.pool = nn.MaxPool2d(kernel_size=3, stride=1, padding=1)
        self.b4 = Conv(c1, c_branch, k=1, s=1)

    def forward(self, x: torch.Tensor) -> torch.Tensor:
        a = self.b1(x)
        b = self.b2(x)
        c = self.b3(x)
        d = self.b4(self.pool(x))
        return torch.cat([a, b, c, d], dim=1).contiguous()


class SEBlock(nn.Module):
    """通道注意力（全卷积实现），避免 AMP 下的 inplace 激活"""

    def __init__(self, c1: int, ratio: int = 16):
        super().__init__()
        c_mid = max(1, c1 // ratio)
        self.avg = nn.AdaptiveAvgPool2d(1)
        self.fc1 = nn.Conv2d(c1, c_mid, kernel_size=1, stride=1, padding=0, bias=True)
        self.act = nn.SiLU(inplace=False)
        self.fc2 = nn.Conv2d(c_mid, c1, kernel_size=1, stride=1, padding=0, bias=True)
        self.gate = nn.Sigmoid()

    def forward(self, x: torch.Tensor) -> torch.Tensor:
        w = self.avg(x)
        w = self.fc2(self.act(self.fc1(w)))
        w = self.gate(w)
        return x * w


class FusionAttention(nn.Module):
    """双模态融合（RGB/IR）：Inception+SE 后逐元素相加，输出通道与输入一致"""

    def __init__(self, c1: int):
        super().__init__()
        self.inc_rgb = Inception(c1)
        self.inc_ir = Inception(c1)
        self.se_rgb = SEBlock(c1)
        self.se_ir = SEBlock(c1)

    def forward(self, x: list[torch.Tensor] | tuple[torch.Tensor, torch.Tensor]) -> torch.Tensor:
        x_rgb, x_ir = x
        fr = self.se_rgb(self.inc_rgb(x_rgb))
        fi = self.se_ir(self.inc_ir(x_ir))
        return (fr + fi).contiguous()


class FeatureAttentionConcat(nn.Module):
    """改进版特征注意力拼接模块（FA-Concat）

    设计目标：
    - 仅做特征增强与去噪：分别对 RGB 与 IR 进行 Inception 多分支特征提取与 SE 通道注意力加权；
    - 不做融合（不逐元素相加）：避免信息的不可逆丢失；
    - 最终输出沿通道维进行拼接（concat），通道数为单模态的 2 倍；
    - 从输入/输出形态上与基线中的纯 Concat 保持一致，方便替换与对比。
    """

    def __init__(self, c1: int):
        """构造函数

        参数：
        - c1：单模态输入通道数（必须可被 4 整除，约束由 Inception 内部断言保证）
        """
        super().__init__()
        # 两路模态共享相同的增强结构：Inception + SE
        self.inc_rgb = Inception(c1)
        self.inc_ir = Inception(c1)
        self.se_rgb = SEBlock(c1)
        self.se_ir = SEBlock(c1)

    def forward(self, x: list[torch.Tensor] | tuple[torch.Tensor, torch.Tensor]) -> torch.Tensor:
        """前向计算

        输入：
        - x：包含两路模态特征的列表或元组 (x_rgb, x_ir)，形状均为 [B, C, H, W]

        输出：
        - 沿通道维拼接后的张量，形状为 [B, 2*C, H, W]
        """
        x_rgb, x_ir = x
        # 对两路模态各自进行特征增强与噪声抑制
        fr = self.se_rgb(self.inc_rgb(x_rgb))  # 增强后的 RGB 特征
        fi = self.se_ir(self.inc_ir(x_ir))    # 增强后的 IR 特征
        # 不做加法融合，直接沿通道拼接，保持信息可逆与完整
        return torch.cat([fr, fi], dim=1).contiguous()


class CrossModalSE(nn.Module):
    """跨模态 SE 注意力模块：联合感知 RGB 与 IR 的全局信息后分别生成两路权重

    设计动机：
    - 传统 SE 为单入单出，权重仅由本模态特征决定，无法在 RGB/IR 质量不一致时做抑制/放大；
    - 跨模态 SE 同时接收两路特征，先做全局池化得到两个描述符，再在通道维进行早期拼接，
      通过 1x1 卷积 MLP 联合推理，输出长度为 2*C 的权重，并拆分回两路分别加权。
    """

    def __init__(self, c1: int, ratio: int = 16):
        super().__init__()
        # 这里的中间层通道数基于联合通道数 2*C 进行压缩，避免过拟合同时保留足够表达能力
        c_joint = 2 * c1
        c_mid = max(1, c_joint // ratio)
        # 全局平均池化将每路特征压缩为 [B, C, 1, 1]
        self.avg = nn.AdaptiveAvgPool2d(1)
        # 1x1 全卷积实现的两层 MLP：降维 -> SiLU -> 升维 -> Sigmoid 门控
        self.fc1 = nn.Conv2d(c_joint, c_mid, kernel_size=1, stride=1, padding=0, bias=True)
        self.act = nn.SiLU(inplace=False)
        self.fc2 = nn.Conv2d(c_mid, c_joint, kernel_size=1, stride=1, padding=0, bias=True)
        self.gate = nn.Sigmoid()

    def forward(self, x_rgb: torch.Tensor, x_ir: torch.Tensor) -> tuple[torch.Tensor, torch.Tensor]:
        """前向：联合两路全局信息，生成并分发权重到各自模态

        输入：
        - x_rgb, x_ir: 两路特征，形状均为 [B, C, H, W]

        输出：
        - 分别加权后的两路特征，形状与输入一致
        """
        # 分别做全局平均池化得到两路描述符
        r = self.avg(x_rgb)
        i = self.avg(x_ir)
        # 在通道维度早期融合，形成联合上下文向量 [B, 2*C, 1, 1]
        joint = torch.cat([r, i], dim=1).contiguous()
        # 联合推理得到长度为 2*C 的权重向量，并用 Sigmoid 映射到 0~1
        w = self.fc2(self.act(self.fc1(joint)))
        w = self.gate(w)
        # 拆分并分发到两路模态，按通道广播相乘
        w_r, w_i = torch.split(w, x_rgb.size(1), dim=1)
        return (x_rgb * w_r).contiguous(), (x_ir * w_i).contiguous()


class CrossModalFusionAttention(nn.Module):
    """CM-FA-Concat：Inception 提取 + 跨模态 SE 加权 + 通道拼接

    接口保持与 FA‑Concat 一致：输入为 [RGB, IR]，输出为通道维拼接后的张量，通道数为 2*C。
    差异在于权重计算由 CrossModalSE 统一建模两路质量，从而在夜间/雾天等场景自动抑制低质量模态。
    """

    def __init__(self, c1: int):
        super().__init__()
        # 两路独立 Inception 特征提取（保持不变）
        self.inc_rgb = Inception(c1)
        self.inc_ir = Inception(c1)
        # 单个跨模态 SE 单元共享使用
        self.cm_se = CrossModalSE(c1)

    def forward(self, x: list[torch.Tensor] | tuple[torch.Tensor, torch.Tensor]) -> torch.Tensor:
        """前向：先独立提取特征，再做跨模态权重分配，最终拼接输出"""
        x_rgb, x_ir = x
        fr = self.inc_rgb(x_rgb)
        fi = self.inc_ir(x_ir)
        fr_w, fi_w = self.cm_se(fr, fi)
        return torch.cat([fr_w, fi_w], dim=1).contiguous()
```

### 8.2 主 YAML 完整内容（`dualbackbone_CM-FA-Concat.yaml`）

文件位置：`src/cfg/model/dualbackbone_CM-FA-Concat.yaml`

```yaml
# 双主干改进融合 OBB 模型配置（CM‑FA‑Concat：跨模态 SE 加权 + 通道拼接）
# 说明：
# - 基于 `dualbackbone_FA-Concat.yaml`，仅将颈部三处的 `FeatureAttentionConcat` 替换为 `CrossModalFusionAttention`；
# - `CrossModalFusionAttention` 通过跨模态 SE 在联合全局上下文上计算权重，分别加权 RGB/IR 后再沿通道拼接；
# - 输出形态与纯 Concat/FA‑Concat 保持一致（通道为两路之和），因此后续 `C2f` 与 OBB 头的配置不变。

# 类别数（与数据集一致）
nc: 5

# 输入通道固定为 6（RGB+IR）
ch: 6

# 复用官方的尺度表（depth, width, max_channels）以兼容不同模型规模的自动缩放
scales:
  n: [0.33, 0.25, 1024]
  s: [0.33, 0.50, 1024]
  m: [0.67, 0.75, 768]
  l: [1.00, 1.00, 512]
  x: [1.00, 1.25, 512]

# 双主干（Backbone）
backbone:
  # 0: 恒等输入，占位并明确分支起点
  - [-1, 1, IdentityInput, []]
  # 1: 从恒等输入选择 RGB（三通道）
  - [-1, 1, ModalitySelector, [1]]
  # 2: 从恒等输入选择 IR（三通道）
  #    注意：此处使用 -2 以确保选择同一上游输入（而非上一层的输出），配合解析器的负索引行为达到复用输入的效果。
  - [-2, 1, ModalitySelector, [2]]

  # ===== RGB 分支（从 1 开始） =====
  - [1, 1, Conv, [64, 3, 2]]
  - [-1, 1, Conv, [128, 3, 2]]
  - [-1, 3, C2f, [128, True]]
  - [-1, 1, Conv, [256, 3, 2]]
  - [-1, 6, C2f, [256, True]]  # RGB-P3
  - [-1, 1, Conv, [512, 3, 2]]
  - [-1, 6, C2f, [512, True]]  # RGB-P4
  - [-1, 1, Conv, [1024, 3, 2]]
  - [-1, 3, C2f, [1024, True]]
  - [-1, 1, SPPF, [1024, 5]]    # RGB-P5

  # ===== IR 分支（从 2 开始） =====
  - [2, 1, Conv, [64, 3, 2]]
  - [-1, 1, Conv, [128, 3, 2]]
  - [-1, 3, C2f, [128, True]]
  - [-1, 1, Conv, [256, 3, 2]]
  - [-1, 6, C2f, [256, True]]  # IR-P3
  - [-1, 1, Conv, [512, 3, 2]]
  - [-1, 6, C2f, [512, True]]  # IR-P4
  - [-1, 1, Conv, [1024, 3, 2]]
  - [-1, 3, C2f, [1024, True]]
  - [-1, 1, SPPF, [1024, 5]]    # IR-P5

# 颈部与头部（Head）：各尺度 CM‑FA‑Concat 融合 + OBB 输出
# 索引说明：截至上方主干，层索引依次为 0..22，其中：
# - RGB-P3: 7，RGB-P4: 9，RGB-P5: 12
# - IR-P3: 17，IR-P4: 19，IR-P5: 22
head:
  # 融合 P3（CrossModalFusionAttention 替换 FA‑Concat）
  - [[7, 17], 1, CrossModalFusionAttention, []]
  - [-1, 3, C2f, [256]]       # 24: h_p3

  # 融合 P4
  - [[9, 19], 1, CrossModalFusionAttention, []]
  - [-1, 3, C2f, [512]]       # 26: h_p4

  # 融合 P5
  - [[12, 22], 1, CrossModalFusionAttention, []]
  - [-1, 3, C2f, [1024]]      # 28: h_p5

  # 三尺度 OBB 检测头
  - [[24, 26, 28], 1, OBB, [nc, 1]]
```

### 8.3 本次会话改动清单（描述版）

1. **新增/改造训练入口**  
   - `src/trainning/train_manager.py`：增加基于 `results.csv` 的训练完成判断与自动恢复逻辑；  
   - `src/trainning/cm_fa_transfer_train.py`：补齐 `run_dir` 与 `total_epochs` 给训练管理器使用。

2. **CM‑FA 迁移训练超参定稿**  
   - `epochs=160`、`patience=0`、`freeze=10`、`close_mosaic=16`、`close_dropout=16`、`conf=0.25`、`iou=0.75`、`max_det=300`；
   - 输出目录固定为 `models/posttrain/CM-FA-Transferred`。

3. **冒烟脚本退出策略**  
   - `crossmodal_fusion_attention_quick_train.py`：进入验证阶段即主动退出，避免 NMS 过慢拖死。

4. **SRC 脚本归档结构调整**  
   - 已新增 `src/trainning/archive/`、`src/testing/archive/`、`src/tools/archive/`；  
   - 历史训练脚本与部分评估脚本已迁入归档目录。

5. **补丁文档持续更新**  
   - 已补充 SRC 目录结构、训练入口定义与研究助理所需内容。

### 8.4 Modality Dropout 实现位置与接入方式

**当前实现位置**：`ultralytics-8.2/ultralytics/models/yolo/detect/train.py` 的 `DetectionTrainer.preprocess_batch`。  
**接入方式**：通过训练参数 `drop_prob_rgb`、`drop_prob_ir`、`close_dropout` 直接控制。  

```python
    def preprocess_batch(self, batch):
        batch["img"] = batch["img"].to(self.device, non_blocking=True).float() / 255
        
        # Modality Dropout (RGB+IR)
        # Apply only if configured and not in the closing epochs
        if (self.args.drop_prob_rgb > 0 or self.args.drop_prob_ir > 0) and \
           (self.epochs - self.epoch > self.args.close_dropout):
            
            imgs = batch["img"]
            # Only apply to 6-channel input (3 RGB + 3 IR)
            if imgs.shape[1] == 6:
                B = imgs.shape[0]
                # Generate random masks
                rand_rgb = torch.rand(B, device=imgs.device)
                rand_ir = torch.rand(B, device=imgs.device)
                
                mask_rgb = rand_rgb < self.args.drop_prob_rgb
                mask_ir = rand_ir < self.args.drop_prob_ir
                
                # Mutex check: prevent dropping both modalities (all black)
                conflict = mask_rgb & mask_ir
                if conflict.any():
                    # For conflicting samples, randomly choose one to KEEP
                    # True: Keep IR (unset mask_ir), False: Keep RGB (unset mask_rgb)
                    resolve_choice = torch.rand(conflict.sum(), device=imgs.device) > 0.5
                    
                    # Update masks based on choice
                    mask_ir[conflict] = mask_ir[conflict] & (~resolve_choice)
                    mask_rgb[conflict] = mask_rgb[conflict] & resolve_choice
                
                # Apply dropout
                if mask_rgb.any():
                    imgs[mask_rgb, 0:3, :, :] = 0.0
                if mask_ir.any():
                    imgs[mask_ir, 3:6, :, :] = 0.0
                    
                batch["img"] = imgs
```

**与 `close_dropout=10` 的关系**  
当 `self.epochs - self.epoch <= close_dropout` 时 Dropout 自动关闭；因此 `close_dropout=10` 表示最后 10 轮不再应用。  

### 8.5 Stage 5 当前训练状态

- 目前 `models/posttrain/` 下 **未发现** `CM-FA-Transferred/` 输出目录；  
- 说明 Stage 5 的正式训练 **尚未启动或未产生输出**（无 `results.csv` 与 `weights/` 迹象）。

### 8.6 `build_confusion_matrix_best.py` 的输出格式

脚本输出位于 `result/<run_name>/`：  
- `confusion_matrix.png`：**未归一化**的原始计数矩阵  
- `confusion_matrix_normalized.png`：**按列归一化**的混淆矩阵  
- `predictions.json`：若 `save_json=True` 则输出预测结果  
