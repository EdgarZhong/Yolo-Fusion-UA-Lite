# 基于正则化与损失重加权的难样本挖掘微调

## 1. 实验背景与目标
**当前问题**：模型存在严重的“过度自信”与“过拟合”现象。表现为正样本置信度极高（>0.8），但未能召回部分难样本（False Negatives），且低置信度阈值策略无效，表明难样本的置信度被过度压制（接近 0）。
**实验目标**：通过引入强正则化手段和调整损失函数权重，打破模型对简单样本的过拟合，强迫模型挖掘并“捞回”处于决策边界边缘的难样本，在保持 mAP 不崩盘的前提下显著提升 Recall。

## 2. 基础环境配置
*   **基础权重 (Model)**: `.../FA-Concat_FPN-PAN_tuned/weights/best.pt` (Epoch 154 SOTA权重)。
    *   *说明：利用其强大的特征提取能力作为热启动。*
*   **分辨率 (ImgSz)**: **640**。
    *   *说明：严禁使用 800。回归 640 以匹配特征尺度，避免 BN 层统计漂移导致的性能下降。*
*   **训练轮次 (Epochs)**: **30**。
    *   *说明：引入了 Dropout 和强正则化，收敛速度会变慢，需比 20 轮稍长以确保拟合。*
*   **批量大小 (Batch)**: 16 (或显存允许的最大值)。

## 3. 核心策略：正则化与反过拟合 (Regularization)

本部分是本次微调的灵魂，旨在解决“模型太自信”的问题。

| 参数项 | 设定值 | 原理说明 |
| :--- | :--- | :--- |
| **dropout** | **0.15** | **关键新增**。在 Head/C2f 层引入 15% 的随机失活。防止特征共适应，强迫模型利用弱特征进行推断。 |
| **label_smoothing**| **0.1** | **关键新增**。将正样本标签从 1.0 平滑为 0.9。惩罚模型的过度自信，使难样本的预测概率分布变宽（从 0 上浮）。 |
| **weight_decay** | **0.001** | (原 0.0005) 加倍权重衰减。强力抑制过拟合，限制权重数值的爆发。 |
| **warmup_epochs** | **0** | 关闭热身，直接进入微调状态。 |

## 4. 损失函数重构 (Loss Reweighting)

本部分旨在解决“漏检”问题，调整优化器的注意力焦点。

| 参数项 | 设定值 | 原理说明 |
| :--- | :--- | :--- |
| **cls (Class Gain)**| **1.5** | (原 0.5) **提升 3 倍**。大幅增加分类错误的惩罚。主要目的是惩罚“将前景误判为背景”的行为，强迫模型在不确定时倾向于认为是物体。 |
| **box (Box Gain)** | **8.5** | (原 7.5) 略微提升。保证在召回更多框的同时，回归精度不下降，确保 IoU 能过阈值。 |
| **fl_gamma** | **1.5** | 保持默认。鉴于已大幅提升 cls 权重，暂不调整 Gamma 以免梯度爆炸。 |

## 5. 数据增强：真实分布适应 (Real-world Adaptation)

旨在让模型适应无拼接、无色彩干扰的真实数据，同时保留几何泛化能力。

| 参数项 | 设定值 | 原理说明 |
| :--- | :--- | :--- |
| **mosaic** | **0.0** | 关闭。消除拼接伪影，专注于真实目标尺寸与上下文。 |
| **mixup** | **0.0** | 关闭。 |
| **hsv_h/s/v** | **0.0** | 关闭。保护双模态物理特征。 |
| **degrees** | **0.0** | 关闭旋转增强。保护 OBB 角度精度。 |
| **translate** | **0.1** | 保留。防止过拟合绝对位置。 |
| **scale** | **0.2** | 保留。防止过拟合特定大小，配合 weight_decay 起效。 |
| **fliplr** | **0.5** | 保留。 |

## 6. 优化器配置
*   **Optimizer**: `SGD`
*   **lr0**: **0.001**
    *   *说明：介于接续训练 (0.0025) 和微小修补 (0.0001) 之间。因为引入了 Dropout 和 Weight Decay，学习率不能太低，否则冲不出局部最优。*
*   **lrf**: **0.05** (最终降至 0.00005)。

---

## 7. 实施脚本代码块 (`src/trainning/final_recall_train.py`)

## 8. 预期结果与验证标准

1.  **训练曲线**：Loss 可能不会像之前那样骤降，甚至初期会比之前高（因为引入了 Dropout 和强 cls 约束），这是正常的。关注 Validation Recall 是否在震荡中上升。
2.  **验证标准**：训练结束后，请使用以下参数进行测试：
    *   `conf=0.25` + `iou=0.75` (你的统一标准)
    *   **重点关注**：`metrics/recall(B)` 是否突破了 0.706 的瓶颈。哪怕 mAP 持平，只要 Recall 上升，即视为实验成功（解决了“难样本学不到”的问题）。