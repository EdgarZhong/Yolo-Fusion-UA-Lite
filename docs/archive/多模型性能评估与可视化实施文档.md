# 多模型性能评估与可视化实施文档

## 1. 概述 (Overview)

为了全面评估 YOLO-Fusion-UA-Lite 项目中不同架构设计、融合模块及训练策略的有效性，本阶段将构建一个包含 **7 个模型** 和 **6 个维度** 的综合评价体系。通过雷达图（Radar Chart）和详细性能表，直观展示从单模态基线到 SOTA 模型的演进路径，以及不同消融实验对模型行为的具体影响。

## 2. 评估对象注册表 (Model Registry)

我们将以下 7 个模型纳入最终评估体系。请确保对应路径下的权重文件存在。

| 编号 | 模型代号 (Code Name) | 架构特征 | 训练策略 | 角色定义 |
| :--- | :--- | :--- | :--- | :--- |
| **M0** | **IR-YOLOv8n** | 单模态 / 完整 YOLO | Pretrained | **绝对基线 (Baseline)** |
| **M1** | **Dual-Easy-Concat** | 双模态 / **无 Neck** / Concat | Scratch | **简单融合对照** |
| **M2** | **Dual-FA-COncat(without neck)** | 双模态 / **无 Neck** / FA | Scratch | **注意力模块对照 (No Neck)** |
| **M3** | **FA-Concat (Scratch)** | 双模态 / **有 Neck** / FA | Scratch | **完整架构对照** |
| **M4** | **CM-FA (Scratch)** | 双模态 / 有 Neck / **CM-FA** | Scratch | **高级模块消融** |
| **M5** | **FA-Concat (Tuned)** | 双模态 / 有 Neck / FA | **Weight Transfer + 200E** | **最佳性能 (SOTA)** |
| **M6** | **FA-Concat (Reg)** | 双模态 / 有 Neck / FA | **Regularized Finetune** | **难样本挖掘策略消融** |

---

## 3. 评估维度定义 (Dimensions & Metrics)

所有指标均经过归一化处理以适配雷达图展示，物理含义如下：

1.  **mAP50**: 反映模型综合检测能力的权威指标。
2.  **mAP50-95**: 反映模型在高 IoU 阈值下的定位精度与回归质量。
3.  **Recall**: 查全率，衡量模型对难样本和微小目标的捕获能力。
4.  **Precision**: 查准率，衡量模型对抗背景噪声和抑制虚警的能力。
5.  **Inference FPS**: 推理速度。基于 10% 测试集测得的每秒处理帧数（含预处理+推理+NMS）。
6.  **Convergence Efficiency**: 收敛效率。
    *   **定义**：$100 / (\text{Epochs to reach mAP50=0.6})$
    *   *说明：衡量模型进入可用状态的速度。权重迁移模型的该指标将显著高于随机初始化模型。*

---

## 4. 实施步骤 A：推理速度基准测试

由于现有日志缺乏统一环境下的速度数据，需运行专用脚本获取 FPS。

**脚本路径**：`src/testing/benchmark_speed_subset.py`
**执行逻辑**：使用测试集的前1000组数据，统一输入 640px，模拟真实推理场景。
**脚本顶部创建路径集合放所有权重的路径，运行脚本一次性完成所有模型的推理速度测试**。

---

## 5. 实施步骤 B：数据收集与整合

请查阅/result下各模型的精度数据及上述脚本的输出，创建表格填入文件`多模型结果汇总.csv`，作为绘图数据源。

**数据收集模板**：(此为模板，数据不真实，填入真实数据时必须精确到小数点后5位)

| 模型代号 | mAP50 | mAP95 | Recall | Precision | FPS (实测) | 收敛轮次 (Epochs to 0.6) |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **M0** | 0.740 | 0.550 | 0.680 | 0.759 | *待测* | *< 10* |
| **M1** | *Log* | *Log* | *Log* | *Log* | *待测* | *Log* |
| **M2** | *Log* | *Log* | *Log* | *Log* | *待测* | *Log* |
| **M3** | 0.730 | 0.589 | 0.677 | 0.747 | *待测* | ~120 |
| **M4** | 0.718 | 0.576 | 0.673 | 0.734 | *待测* | ~130 |
| **M5** | **0.762** | **0.615** | **0.704** | **0.785** | *待测* | **< 20** |
| **M6** | 0.746 | 0.604 | 0.701 | 0.763 | *待测* | *N/A (继承)* |

---

## 6. 实施步骤 C：雷达图可视化脚本

**脚本路径**：`src/tools/plot_radar_final.py`
**功能**：自动归一化数据，生成六维雷达图。
配色顺序必须和plot_training.py中的一致
---

## 7. 预期交付物

完成上述步骤后，您将获得：
1.  **控制台输出**：包含 7 个模型真实推理延迟与 FPS 的基准数据。
2.  **radar_chart_final.png**：一张展示了模型从“单模态”到“双模态SOTA”演进过程的六维雷达图。
    *   *预期视觉效果*：**M5 (Green)** 应当在除 FPS 外的所有维度包围其他 Scratch 模型，且在收敛效率轴上极度突出。
3.  **模型对比汇总表**：一份包含绝对数值的详细csv表格，用于报告正文。