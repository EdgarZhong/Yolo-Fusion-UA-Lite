# 编码设计文档：DroneVehicle 数据集自适应白边清洗工具

## 1. 概述
在 `src/data_preprocess/` 目录下实现本工具，用于清除 `DroneVehicle` 数据集图像边缘的白色无效填充。
### 1.1 设计目标
构建一个高性能的离线数据预处理系统，旨在彻底清除 `DroneVehicle` 数据集图像边缘的白色无效填充。系统需确保在双模态（RGB/IR）严格对齐的前提下，实现“宁可错杀、绝不放过”的白边去除策略，并同步更新 OBB 标签坐标，输出标准化的纯净数据集。

### 1.2 输入输出定义
*   **源数据根目录**: `data/` (只读)
*   **目标数据根目录**: `data_croped/` (写出)
*   **处理范围**: 全量覆盖 `train`, `val`, `test` 子集。

## 2. 目录结构与映射规范

系统需在运行时动态构建目标路径，严格保持与源数据集一致的目录树结构。

**路径映射规则：**

| 数据类型 | 源路径模式 (Source) | 目标路径模式 (Target) |
| :--- | :--- | :--- |
| **可见光图像** | `data/{subset}/{subset}img/` | `data_croped/{subset}/{subset}img/` |
| **红外图像** | `data/{subset}/{subset}imgr/` | `data_croped/{subset}/{subset}imgr/` |
| **OBB 标签** | `data/{subset}/{subset}labels_yolo_obb/` | `data_croped/{subset}/{subset}labels_yolo_obb/` |

*注：`{subset}` 遍历 `train`, `val`, `test`。*

## 3. 核心算法逻辑

### 3.1 边缘扫描与交集裁切 (Scan & Intersect)

为确保白边被彻底移除，算法采用“双模态独立扫描，取有效区域交集”的策略。

**处理流程：**

1.  **图像加载与灰度化**:
    *   并行读取同名的 RGB 图像 ($I_{rgb}$) 和 IR 图像 ($I_{ir}$)。
    *   转换为单通道灰度图。

2.  **二值化掩膜生成**:
    *   设定白边阈值 $T = 250$。
    *   生成掩膜：像素值 $< T$ 标记为 **True** (有效内容)，$\ge T$ 标记为 **False** (白边)。

3.  **轴向投影扫描 (Axis Projection Scanning)**:
    *   **行扫描**: 对掩膜矩阵按行求逻辑或 (OR)，定位出现 True 的首行索引 ($Y_{min}$) 和末行索引 ($Y_{max}$)。
    *   **列扫描**: 对掩膜矩阵按列求逻辑或 (OR)，定位出现 True 的首列索引 ($X_{min}$) 和末列索引 ($X_{max}$)。
    *   分别得出 RGB 的有效范围 $(y1_{rgb}, y2_{rgb}, x1_{rgb}, x2_{rgb})$ 和 IR 的有效范围 $(y1_{ir}, y2_{ir}, x1_{ir}, x2_{ir})$。

4.  **交集融合决策 (Intersection Strategy)**:
    *   **原则**: 取两个模态中**更靠内**的边界值，即计算有效区域的交集，最大化裁切范围。
    *   $Top = \max(y1_{rgb}, y1_{ir})$
    *   $Bottom = \min(y2_{rgb}, y2_{ir})$
    *   $Left = \max(x1_{rgb}, x1_{ir})$
    *   $Right = \min(x2_{rgb}, x2_{ir})$
    *   **最终裁切参数**:
        *   起始点: $(Left, Top)$
        *   新宽度: $W_{new} = Right - Left$
        *   新高度: $H_{new} = Bottom - Top$

### 3.2 标签坐标重映射 (Coordinate Remapping)

对 YOLO-OBB 格式标签 `class cx cy w h angle` 进行刚性变换。

**变换公式**:
设原图尺寸为 $(W_{old}, H_{old})$，裁切偏移为 $(dx, dy) = (Left, Top)$。

1.  **还原绝对坐标**:
    *   $cx_{abs} = cx_{norm} \times W_{old}$
    *   $cy_{abs} = cy_{norm} \times H_{old}$
    *   $w_{abs} = w_{norm} \times W_{old}$
    *   $h_{abs} = h_{norm} \times H_{old}$

2.  **坐标平移**:
    *   $cx'_{abs} = cx_{abs} - dx$
    *   $cy'_{abs} = cy_{abs} - dy$

3.  **重归一化 (至新尺寸)**:
    *   $cx'_{norm} = cx'_{abs} / W_{new}$
    *   $cy'_{norm} = cy'_{abs} / H_{new}$
    *   $w'_{norm} = w_{abs} / W_{new}$
    *   $h'_{norm} = h_{abs} / H_{new}$

4.  **角度保持**: $\theta' = \theta$

### 3.3 数据分组统计

建立裁切元数据索引，用于后续的数据完整性校验或特定实验分析。

*   **键 (Key)**: 裁切特征字符串，格式建议为 `"x{Left}_y{Top}_w{Wnew}_h{Hnew}"`。
*   **值 (Value)**: 列表，存储命中该裁切规则的文件相对路径 (如 `train/0001`)。
*   **输出**: 处理完成后，将统计字典序列化为 JSON 文件存储于 `data_croped/crop_meta.json`。

## 4. 系统架构与性能设计

### 4.1 并行处理模型
鉴于图像编解码 (Codec) 是主要耗时点，且各图像对处理相互独立，采用 **多进程 (Multiprocessing)** 架构。

*   **Worker**: 负责单一图像对的读取、计算、裁切、保存及标签转换。
*   **Manager**: 负责任务分发 (Pool)、进度条维护 (tqdm) 及统计数据的聚合。

### 4.2 执行逻辑伪代码

```text
Process Global:
    定义源路径 ROOT_SRC, 目标路径 ROOT_DST
    定义原图尺寸 ORIGIN_W=840, ORIGIN_H=712
    初始化结果收集器 ResultsList

    Main:
        For subset in [train, val, test]:
            创建目标目录结构
            收集该子集下所有 RGB 图片路径列表 ImagePairs
            
            # 并行执行
            Parallel_Map(ProcessSinglePair, ImagePairs) -> SubResults
            ResultsList.extend(SubResults)
        
        聚合 ResultsList 到分组字典 CropMeta
        写入 data_croped/crop_meta.json

Function ProcessSinglePair(image_path_rgb):
    推导 image_path_ir, label_path_src, 以及对应的输出路径
    
    # 1. IO 读取
    img_rgb = Decode(image_path_rgb)
    img_ir = Decode(image_path_ir)
    
    # 2. 快速扫描 (Numpy Vectorization)
    mask_rgb = img_rgb < 250
    mask_ir = img_ir < 250
    rgb_box = GetValidBox(mask_rgb) # argwhere(any(axis=0/1))
    ir_box = GetValidBox(mask_ir)
    
    # 3. 融合决策
    crop_params = Intersect(rgb_box, ir_box)
    
    # 4. 图像裁切与写入
    Save(Crop(img_rgb, crop_params), out_path_rgb)
    Save(Crop(img_ir, crop_params), out_path_ir)
    
    # 5. 标签处理
    If label_exists:
        new_label_data = Transform(label_path_src, crop_params)
        Save(new_label_data, out_path_label)
    Else:
        TouchEmptyFile(out_path_label)
        
    Return (file_id, crop_params_string)
```

## 5. 异常边界控制

1.  **全白图像处理**: 若某张图被判定为全白（无有效像素），或交集区域宽/高 <= 0，这属于严重的数据异常。策略：**跳过该样本**，并将其 ID 记录到 `error.log`，不生成到新数据集中。
2.  **标签越界**: 经过裁切后，若目标中心点移出图像边界（$cx', cy' < 0$ 或 $> 1$），视为该目标已被完全裁切掉（虽然按照逻辑不应发生，因为只裁切白边）。策略：**在标签生成时丢弃该目标行**。
3.  **单模态文件缺失**: 严格断言文件成对存在。若不匹配，抛出异常终止程序，人工介入检查数据完整性。